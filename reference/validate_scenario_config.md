# Validate a Scenario Configuration

Checks that a scenario configuration object has valid structure.

## Usage

``` r
validate_scenario_config(config)
```

## Arguments

- config:

  A scenario_config object.

## Value

TRUE if valid, otherwise throws an error.
