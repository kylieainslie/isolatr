# Convert Data Frame to Named Vector

Converts a two-column data frame to a named vector.

## Usage

``` r
to.named.vector(df)
```

## Arguments

- df:

  Data frame with two columns. First column becomes names, second column
  becomes values.

## Value

Named numeric vector.
